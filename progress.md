Original prompt: 回答履歴のお題行全体をクリック可能にし、全員回答後もホストが公開するまで非公開にする。正体選択は自分以外のユーザー名だけを装飾した一覧へ表示し、プレースホルダーを「回答者を選択」にする。確定済み履歴をキャッシュして画面の再読込によるちらつきを防ぐ。

## 2026-09-12

- `round.publish` を追加。最後の提出では公開せず、全員提出済みの場合だけホスト操作で `DISCUSSING` へ進む。
- 回答状況にホストの「回答を公開する」と参加者の公開待ち表示を追加。
- 回答履歴のお題行全体をボタン化。
- 正体候補を本人以外の `username` に限定し、PC / Mobileとも装飾した選択シートに統一。
- 履歴をセッション・公開範囲・表示種別・対象ごとにメモリキャッシュ。お題別は再利用し、回答者別は表示を保持したまま `historyVersion` 更新時に再取得。
- `npm run typecheck`、`npm test`（15件）、`npm run build` 成功。キャッシュ再利用・差分結合の単体テストを含む。
- 実ブラウザ2ユーザーで、1人目の本文非漏洩、2 / 2後もカード0件、非ホストの公開待ち表示、ホスト公開後の両画面同時表示を確認。
- PCでお題カード右下余白から選択できること、PC / 390×844で装飾済み候補シートを確認。候補は `ren`〜`kai` の9ユーザー名だけで本人`yui`を含まない。ブラウザ警告・エラーなし。
- 実データでも表示名`DisplayBeta`ではなくユーザー名`betaid`だけが候補となり、本人`alphaid`を含まないことを確認。

## TODO

- Discord実機iframeでの操作確認は未実施。
